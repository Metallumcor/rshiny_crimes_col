% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_models.R
\encoding{UTF-8}
\name{lagpad}
\alias{lagpad}
\title{Inclusión de columnas con rezago.}
\usage{
lagpad(x, k)
}
\arguments{
\item{x}{Un vector de números.}

\item{k}{La cantidad de rezagos que se adicionan como columnas. Puede
ser cualquier número entero diferente de cero.}
}
\value{
Un vector del tamaño de $x$ con posibles entradas NA.
}
\description{
A partir de un vector numérico, crea otros que contienen
    la variable rezagada p periodos. Se crean NA por construcción
    por lo cual debe tenerse esto en consideración al momento de evaluar
    el modelo.
}
\examples{
# Simular un AR(1)
X <- stats::arima.sim(list(order(p=0.7)),n = 100)
# Convertir a nÃºmerico
X <- as.numeric(X)
# Crear el vector con un rezago
X_1 <- lagpad(X,1)
X_1[1:5]
}
